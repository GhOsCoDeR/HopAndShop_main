NEXT_PUBLIC_VERCEL_URL=${VERCEL_URL}
SKIP_PREFLIGHT_CHECK=true
NODE_ENV=production

# Database connection - will be replaced by Vercel environment variables
# Use file-based SQLite database for development (will be ignored in production)
DATABASE_URL="file:./dev.db" 